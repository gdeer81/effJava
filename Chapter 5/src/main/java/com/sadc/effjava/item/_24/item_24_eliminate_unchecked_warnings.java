package com.sadc.effjava.item._24;

/**
 * Created by k557782 on 9/4/14.
 */
public class item_24_eliminate_unchecked_warnings {
    // unchecked cast warnings, unchecked method invocation warnings, unchecked generic array creation warnings,
    // and unchecked conversion warnings.
    //  Eliminate every unchecked warning that you can.
    // If you eliminate all warnings, you are assured that your code is typesafe, which is a very good thing.
    //If you can’t eliminate a warning, and you can prove that the code that provoked the warning is typesafe, then
    // (and only then) suppress the warning with an @SuppressWarnings("unchecked") annotation. and add comment saying
    // why its typesafe
}
